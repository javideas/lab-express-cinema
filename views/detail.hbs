<div class="card border-0" style="width: 28rem;">
    <img class="card-img-top" src="{{movie.image}}" alt="img for {{movie.title}}">
    <div class="card-body">
        <h5 class="card-title">{{movie.title}}</h5>
        <p class="card-text">{{movie.description}}</p>
        <a href="/beers/{{movie.title}}" class="btn btn-primary">More info 🏭</a>
    </div>
</div>